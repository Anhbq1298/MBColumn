namespace MBColumn.Infrastructure.Math;

public static class PolygonClipper
{
    // Reserved for a future exact stress-block polygon implementation. The MVP uses concrete
    // fiber integration so the solver remains simple, stable, and easy to regression test.
}

