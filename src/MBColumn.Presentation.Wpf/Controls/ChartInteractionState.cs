namespace MBColumn.Presentation.Wpf.Controls;

public sealed class ChartInteractionState
{
    public void Reset()
    {
        // 2D charts are intentionally static auto-fit views. Reset only requests a redraw.
    }
}

