<Window x:Class="MBColumn.Presentation.Wpf.Views.StartUpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MBColumn — Start" Height="320" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="MinWidth" Value="88"/>
        </Style>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF1976D2"/>
    </Window.Resources>

    <Border Background="White" Padding="12">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,8">
            <TextBlock Text="MBColumn" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
            <TextBlock Text=" — Start" FontSize="14" Margin="8,0,0,0" VerticalAlignment="Bottom"/>
        </DockPanel>

        <TextBlock Grid.Row="1" Text="Create a new project or open an existing one to continue." TextWrapping="Wrap"/>

        <Grid Grid.Row="2" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Recent Projects" FontWeight="Bold" Margin="0,0,0,6"/>
                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="6">
                    <ListBox x:Name="RecentList" Height="160">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top">
                <Button x:Name="BtnNew" Content="New Project"/>
                <Button x:Name="BtnOpen" Content="Open Project"/>
                <Button x:Name="BtnCancel" Content="Cancel"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Tip: save projects to make them appear in Recent." FontStyle="Italic" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>