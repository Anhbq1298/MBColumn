<Window x:Class="MBColumn.Presentation.Wpf.Views.SelectSectionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Existing Sections" Height="400" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource PanelBackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Text="Select sections to add to the group:" Margin="0,0,0,10" Foreground="{DynamicResource TextDarkBrush}" />
        
        <ListBox x:Name="SectionsListBox" Grid.Row="1" Background="{DynamicResource LightBackgroundBrush}" BorderBrush="{DynamicResource BorderGreyBrush}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Foreground="{DynamicResource TextDarkBrush}" Margin="2" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" IsCancel="True" Width="70" Margin="0,0,10,0" Style="{StaticResource OutlineButtonStyle}" />
            <Button Content="OK" IsDefault="True" Click="OkButton_Click" Width="70" Style="{StaticResource PrimaryButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
