<Window x:Class="MBColumn.Presentation.Wpf.Views.ProjectNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Project"
        Width="360" SizeToContent="Height"
        Style="{DynamicResource DialogWindowStyle}">
    <Grid Style="{DynamicResource DialogRootGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{DynamicResource DialogHeaderBorderStyle}">
            <StackPanel>
                <TextBlock Text="New Project" Style="{DynamicResource DialogHeaderTitleTextStyle}" />
                <TextBlock Text="Name the project file before creating sections."
                           Style="{DynamicResource DialogHeaderSubtitleTextStyle}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{DynamicResource DialogBodyBorderStyle}">
            <StackPanel>
                <TextBlock x:Name="NameLabel"
                           Text="Project name"
                           Style="{DynamicResource FieldLabelTextStyle}" />
                <TextBox x:Name="NameBox"
                         MinWidth="280"
                         KeyDown="NameBox_KeyDown" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{DynamicResource DialogFooterBarStyle}">
            <StackPanel Style="{DynamicResource DialogActionPanelStyle}">
                <Button Content="Cancel"
                        Width="88"
                        IsCancel="True"
                        Click="Cancel_Click"
                        Style="{DynamicResource DialogActionButtonStyle}"
                        Margin="0,0,8,0" />
                <Button Content="OK"
                        Width="88"
                        IsDefault="True"
                        Click="OK_Click"
                        Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
