# Theme Collections

This folder holds grouped WPF resource dictionaries that sit above raw tokens.

- `Theme.Windows.xaml` keeps shared window, popup, dialog header, body, and footer styles.
- Add future collections here when they represent a reusable UI pattern across multiple views.
- Keep primitive colors, font sizes, and control defaults in the parent `Themes` files.
