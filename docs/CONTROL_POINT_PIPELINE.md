# Control Point Pipeline

`ControlPointBuilderService` builds the diagram-ready points from `InteractionSurface`, `LoadDemand`, selected PM angle, selected axial load, unit system, and `RatioResult`.

## Reference Behaviors Used

The reference/decompiled tree was inspected for workflow and chart behavior, especially:

- `StructurePoint.Products.Column.FailureSurface.Model.FailureSurface`
- `StructurePoint.Products.Column.FailureSurface.Core.ModelVisualizationController`
- `StructurePoint.CoreAssets.AppManager.Column.Reporting.Diagrams.Drawing.Helpers.DiagramGeneratorHelper`
- `StructurePoint.CoreAssets.AppManager.Column.Reporting.Diagrams.Drawing.MomentMomentDrawingEngine`
- `tools/PmmChartSmoke.Program`

Observed behavior reflected in the new clean-room implementation:

- 36 angular points per surface row.
- Surface rows are angular rings.
- Surface mesh wraps at the row end.
- PM data is built from a selected angle row and the opposite angle row in reverse depth order.
- MM data is interpolated at a selected axial load level.
- Pmax and Pmin reference points are inserted into PM output.
- Demand and governing points are explicitly inserted.
- Display load filtering uses small tolerances and avoids unstable duplicate points.
- The active PM render DTO maps `X = MÎ¸` and `Y = P`, where `MÎ¸ = Mx cosÎ¸ + My sinÎ¸`; Mx-My render DTOs map `X = Mx` and `Y = My`.
- Chart DTO generation removes NaN/Infinity values and duplicate display points before rendering.

## Reference PM Curve Logic Review

Files/classes inspected in the decompiled reference tree:

- `#gMe/#tPe.cs`
  - `#3Oe(...)`: builds `UniCurve[70]` axial-load rows for PM diagrams.
  - `#pPe(...)`: intersects each 36-point `BiCurve` Mx-My boundary with a diagram angle to get positive and negative moment sides.
  - `#oPe(...)` / `#9Oe(...)`: refine upper/lower transition rows by bisection so curve endpoints are not left with a large discontinuity.
- `StructurePoint.CoreAssets.AppManager.Column.Reporting.Diagrams.Drawing.AxialLoadMomentDrawingEngine`
  - `#KEe(...)`: draws PM branches.
  - `#REe(...)` and `#SEe(...)`: extract positive and negative branches, keep them as separate polylines, insert zero-moment crossing points when needed, and preserve axial-load ordering.
  - `#sEe(...)` / `#9Ee(...)`: draw Pmax/Pmin horizontal reference lines and labels.
- `StructurePoint.CoreAssets.AppManager.Column.Reporting.Diagrams.Drawing.Helpers.GridLinesCreator`
  - Generates gridlines and tick marks from major intervals and `GridLinesDivision` subdivisions around the axes.
- `StructurePoint.CoreAssets.AppManager.Column.Reporting.Diagrams.Drawing.Helpers.DiagramGeneratorHelper`
  - Applies tolerances for selected/demand load matching and load-point filtering.

Reference behavior reflected in this implementation:

- PMx/PMy are generated as separate positive and negative branches, not a single stitched loop.
- Branch points are grouped by axial/depth row, ordered from compression toward tension by descending `P`, and consecutive duplicate display points are filtered.
- Pmax/Pmin are inserted as horizontal reference lines.
- Demand and governing points stay separate from capacity branches.
- Major/minor ticks are generated by a 1-2-5 nice-number algorithm with five minor divisions per major interval.

Current differences and assumptions:

- The reference computes exact line intersections through each Mx-My `BiCurve` for an arbitrary PM angle. This implementation mirrors that behavior for the two principal PM diagrams by creating constant-P Mx-My rings from the PMM surface, then intersecting each ring with `My = 0` for PMx and `Mx = 0` for PMy.
- Reference endpoint bisection around the first/last plotted PM rows is approximated by interpolating 70 constant-P levels through the available PMM surface rows and filtering near-duplicates. Further refinement can add the same bisection-style endpoint search if stricter parity is required.

## PM Curve Zig-Zag Fix

The jagged PMx/PMy curves came from connecting projected PMM points that did not belong to one ordered outer envelope. Raw interaction points from different neutral-axis angles and depths can contain interior points, duplicated axial levels, and both positive/negative moment branches. Connecting those directly, or selecting max/min values without first creating a constant-P boundary, produces the visible zig-zag teeth.

Reference review:

- `#gMe/#tPe.cs`
  - `#3Oe(...)` generates 70 axial-load rows for PM plotting.
  - `#pPe(...)` intersects each row's 36-point Mx-My `BiCurve` with the selected PM diagram direction and returns positive/negative moment values for that same axial row.
  - `#oPe(...)` and `#9Oe(...)` refine end regions with bisection.
- `AxialLoadMomentDrawingEngine`
  - `#REe(...)` and `#SEe(...)` keep positive and negative branches separate before drawing.
  - `#sEe(...)` and `#9Ee(...)` draw Pmax/Pmin as horizontal reference graphics, not as ordinary curve teeth.

New implementation:

- `PmCurveBuilderService.BuildPmXCurve(...)` and `BuildPmYCurve(...)` first remove invalid points and exclude demand, governing, and reference markers from the capacity polyline.
- PMM control points are grouped by surface theta row (`GroupKey`) and each theta row is interpolated at 70 constant axial-load levels, matching the reference row count used for PM drawing.
- For each constant-P ring:
  - PMx intersects the Mx-My ring with `My = 0`, then selects the positive and negative `Mx` crossings.
  - PMy intersects the Mx-My ring with `Mx = 0`, then selects the positive and negative `My` crossings.
- The positive and negative branches are kept as separate ordered branches. Each branch is sorted consistently by descending axial load before being passed to rendering.
- Consecutive near-duplicate points are removed using tolerances derived from the force and moment ranges.
- Pmax/Pmin are appended only as reference markers for horizontal dashed lines. Demand and governing points remain separate markers and are never inserted into the capacity branch polyline.
- No display-only smoothing is used. The plotted line is the corrected envelope polyline.

## Output Sets

- PMx control points: factored positive/negative P-Mx branches, Pmax, Pmin, demand, governing point.
- PMy control points: factored positive/negative P-My branches, Pmax, Pmin, demand, governing point.
- PM angle control points: factored and nominal P-MÎ¸ branches at the selected navigation angle, Pmax, Pmin, demand, governing point.
- Mx-My control points: factored Mx-My boundary at selected Pu, demand, governing point.
- 3D PMM surface points: factored P-Mx-My grid plus demand and governing point.
- 3D MM slice points: selected-Pu Mx-My slice plus demand and governing point.

The implementation is independently written; the decompiled reference source was consulted only for workflow and chart behavior, not copied or translated.

## Nominal vs Design Capacity Curves

### Definition

Each interaction point stores both **nominal** (unfactored) and **design** (factored) capacity:

| Value | Meaning | Symbol | Source Field |
|-------|---------|--------|--------------|
| Nominal Axial | Raw axial capacity | Pn | `InteractionPoint.Pn` |
| Nominal Moment X | Raw Mx capacity | Mnx | `InteractionPoint.Mnx` |
| Nominal Moment Y | Raw My capacity | Mny | `InteractionPoint.Mny` |
| Design Axial | Factored axial capacity | Ï†Pn | `InteractionPoint.PhiPn` (= Pn Ã— Ï†) |
| Design Moment X | Factored Mx capacity | Ï†Mnx | `InteractionPoint.PhiMnx` (= Mnx Ã— Ï†) |
| Design Moment Y | Factored My capacity | Ï†Mny | `InteractionPoint.PhiMny` (= Mny Ã— Ï†) |

The **phi factor** (Ï†) is depth-dependent and varies from 0.65 (compression-controlled) to 0.90 (tension-controlled) per ACI 318.

### Data Flow

1. `ControlPointBuilderService.ToControl()` computes both sets of unit-converted display values for every interaction point:
   - `DisplayP` / `DisplayMx` / `DisplayMy` â†’ from `PhiPn` / `PhiMnx` / `PhiMny` (design, Ï†-applied)
   - `NominalDisplayP` / `NominalDisplayMx` / `NominalDisplayMy` â†’ from `Pn` / `Mnx` / `Mny` (nominal, no Ï†)

2. `PmCurveBuilderService.BuildCurve()` runs the ring-intersection algorithm twice:
   - **Design pass**: uses `DisplayP` / `DisplayMx` / `DisplayMy` â†’ produces branches with GroupKey `"Positive"` / `"Negative"` and `IsNominal = false`.
   - **Nominal pass**: uses `NominalDisplayP` / `NominalDisplayMx` / `NominalDisplayMy` â†’ produces branches with GroupKey `"NominalPositive"` / `"NominalNegative"` and `IsNominal = true`.

3. `DiagramCanvas2D.DrawCapacity()` renders:
   - Design curves: **solid blue** pen (`RGB(0, 75, 133)`, 1.8pt)
   - Nominal curves: **dashed red** pen (`RGB(200, 40, 40)`, 1.4pt)

4. `LegendRenderer.Draw()` shows:
   - **Design Capacity** (solid blue line sample)
   - **Nominal Capacity** (dashed red line sample, when visible)
   - **Demand** (red dot)
   - **Governing** (green dot)

### PMM Ratio / DCR Check

`RatioCheckService.Check()` uses exclusively **design** (Ï†-factored) values:
- Interpolation at demand axial load uses `PhiPn`
- Capacity moment uses `PhiMnx` / `PhiMny`
- The governing point is selected from the design surface

The **nominal curve is for reference only** and is never used in the PMM ratio / DCR calculation.

### UI Controls

The nominal curve visibility is controlled by:
- `ResultViewModel.ShowNominalCurve` property (default: true)
- "Show nominal capacity" checkbox in the result settings panel
- `DiagramCanvas2D.ShowNominalCurve` dependency property

## Chart Data Flow

The WPF chart layer receives only DTO/control-point data. Display transforms, gridlines, panning, zooming, tooltips, and visual scaling are handled in `MBColumn.Presentation.Wpf.Controls` and do not alter the interaction surface or PMM ratio.

## 3D PMM View â€” Interaction and Performance

### Coordinate System

| Axis | Maps to | Color | Direction |
|------|---------|-------|-----------|
| X | Mx (moment about X) | Red (#C82828) | Horizontal |
| Y | My (moment about Y) | Green (#28A032) | Depth |
| Z | P (axial force) | Blue (#1E50C8) | Vertical |

### Interaction Controls

- **Left Drag**: Rotate / orbit (no Shift required)
- **Mouse Wheel**: Zoom in/out
- **Right Drag**: Pan
- **Middle Drag**: Pan
- **Double-click**: Reset camera to default view

### PM Angle Slice Plane

A green semi-transparent slice plane shows the PM section cut through the 3D PMM surface at the selected PM angle. This plane:
- Rotates according to `SelectedPmAngle` (bound from `PM3DViewModel`)
- Is drawn as a vertical quad through the origin along the direction `(cos Î¸, sin Î¸)`
- Fill: `rgba(60, 200, 90, 0.22)`, Outline: green
- Labeled: `"PM at Î¸ = xx.xÂ°"`
- Can be toggled via `ShowSlicePlane` checkbox

### Axial Load Slice Plane

A blue-grey transparent horizontal slice plane shows the Mx-My cut at the user-selected axial load:
- Controlled by `PM3DViewModel.SelectedAxialLoad`
- Drawn at visual coordinate `Z = SelectedAxialLoad` (display P value, same units as the 3D surface)
- Labeled: `"Mx-My at P = xx.x [force unit]"`
- Can be toggled via `ShowAxialLoadSlice`

### Orientation Cube

`InteractionViewport3D` draws a small non-interactive orientation cube in the bottom-right corner. It reuses the same yaw/pitch camera angles as the main view and labels `Mx` in red, `My` in green, and `P` in blue. The cube is a view aid only and does not participate in engineering calculation or surface data generation.

### Geometry Caching

**Performance optimization**: All geometry is pre-computed once and cached in `CachedScene`. During mouse interaction (rotate/zoom/pan), only the 2D projection is recomputed â€” no geometry rebuild.

Geometry is rebuilt only when:
- `Points` data changes (new calculation result)
- `DemandPoint` or `GoverningPoint` changes
- `ShowSurface`, `ShowWireframe`, or `ShowGrid` toggles change

Slice-only changes do not rebuild the full PMM mesh:
- `SelectedPmAngle` changes only redraw the vertical slice plane and label.
- `SelectedAxialLoad` changes only redraw the horizontal slice plane and label.
- Mouse rotate/zoom/pan only recomputes 2D projection.

Cached data:
- Surface quads: pre-computed (X,Y,Z) tuples for each quad face
- Wireframe segments: pre-computed (X,Y,Z) pairs for each wire
- Slice plane: direction vector and extent
- Demand/Governing: pre-extracted (X,Y,Z) tuples

WPF resources (pens, brushes) are frozen for performance.

## Internal Blue Segment Bug â€” Root Cause and Fix (2026-05-09)

### Symptom
Short horizontal blue line segments appeared near the top (Pmax / compression cap region) of both PMx and PMy diagrams. These were internal to the interaction envelope â€” not part of the true outer boundary.

### Root Cause (Two-Part)

**Part 1 â€” Compression cap applied to PMM surface points**

`ControlPointBuilderService.ToControl()` clamped `PhiPn` to `compressionLimit = 0.80 Ã— PhiPn_max` for ALL diagram types, including the `Pm3D` surface points that feed `PmCurveBuilderService`. This created a **flat P-plateau** at the top of each theta row: multiple consecutive surface points all had the same `DisplayP = compressionLimit` but different `(DisplayMx, DisplayMy)` moment values reflecting their actual depth-of-interaction row.

When `BuildRingAtP` sampled at `p = pMax = compressionLimit`, `InterpolateAtP` found and returned one of these plateau points (the first matching pair), carrying its non-zero `(Mx, My)` as a valid ring intersection point. The 36-angle ring therefore had non-zero moment extent at `Pmax`. The positive and negative intersections of this ring produced a moment value, not zero. These became the **first points** of the Positive and Negative PM branches â€” appearing at `(+M_artifact, Pmax)` and `(-M_artifact, Pmax)` â€” visible as internal horizontal connector segments near the top of the chart.

**Part 2 â€” Interpolation condition matched degenerate same-P pairs**

`InterpolateAtP` used a condition that matched any segment pair where both endpoints' P values encompassed the target `p` within tolerance â€” including degenerate pairs where `a.DisplayP â‰ˆ b.DisplayP â‰ˆ p`. This allowed a flat-plateau pair to satisfy the check and return an arbitrary `(Mx, My)` from either endpoint.

### Fix Applied

1. **`ControlPointBuilderService.BuildSurface`** â€” Changed to pass `double.MaxValue` as `compressionLimit` for surface (Pm3D) points. This preserves the raw uncapped `PhiPn` for every theta row, ensuring each theta row has a strictly monotone-descending P profile from the compression end to the tension end. The compression cap (`0.80 Ã— Pmax`) is retained only for the traditional PM diagram branch (`BuildPm`), which is its correct domain.

2. **Current 3D display behavior** â€” The 3D design PMM surface is intentionally ACI-capped so the rendered design surface matches the PMx/PMy design compression limit. `PmCurveBuilderService.InterpolateAtP` rejects degenerate same-P plateau segments, so the PM curve builder does not turn the capped top plateau into internal curve artifacts.

3. **`PmCurveBuilderService.InterpolateAtP`** â€” Strengthened the bracket check to require `nonDegenerate = a.DisplayP - b.DisplayP > tolerance`. This explicitly rejects degenerate pairs where both endpoints are at the same P level.

### Result
- PMx and PMy diagrams show only the true outer interaction envelope.
- No internal blue connector segments near Pmax.
- Pmax/Pmin dashed reference lines remain separate from the capacity polyline.
- Demand and governing points remain separate markers.

## ACI Design Capacity Trim Line

### Definition

Per ACI 318-19 Â§22.4.2.1, the maximum nominal axial strength used for design of tied columns is limited to:

    Pn,max = 0.80 Ã— P0

where P0 = 0.85 Ã— f'c Ã— (Ag - Ast) + fy Ã— Ast (pure compression strength).

The **design** maximum is:

    Ï†Pn,max = Ï† Ã— 0.80 Ã— P0 = 0.80 Ã— max(Ï†Pn)

This creates a horizontal trim line at the top of the design capacity curve. Above this level, the design curve does not exist. Below this level, the section still has non-zero moment capacity, so the trim line connects the min/max design moment envelope points at `P = Ï†Pn,max`.

### Implementation

1. `ControlPointBuilderService.Build()` computes `compressionLimit = 0.80 Ã— max(Ï†Pn)` and converts it to display units as `compressionLimitDisplay`.

2. `DiagramControlPointSet` carries `DesignCompressionLimitDisplay` to downstream services.

3. `PmCurveBuilderService.BuildCurve()` uses this limit:
   - **Design curve**: `designPMax = min(rawPMax, designCompressionLimitDisplay)` â€” the top of the 70-level scan is at the ACI limit, not at the raw maximum.
   - **Nominal curve**: `nomPMax = max(NominalDisplayP)` â€” no cap, uses the true Pn maximum.

4. At `designPMax`, the ring-intersection algorithm produces non-zero moment extent â†’ the top points of the positive and negative branches form the horizontal cap.

5. The merged envelope loop architecture naturally draws this cap: the positive branch's topmost point connects to the negative branch's topmost point when the loop transitions at Pmax.

### Valid vs Invalid Horizontal Lines

| Attribute | Valid ACI trim cap | Invalid internal segment |
|-----------|-------------------|------------------------|
| P level | Exactly at `0.80 Ã— max(Ï†Pn)` | Below or variable |
| Endpoints | Min/max moment at that P | Arbitrary surface points |
| Curve | Part of design capacity (solid blue) | Connects interior points |
| Source | Ring interpolation at capped Pmax | Plateau artifact |

### spColumn.exe Reference

Files inspected in the decompiled reference:
- `#gMe/#tPe.cs` â€” `#3Oe()`: Builds 70 axial-load rows. The design curve uses `PhiPn` values that are already capped at `0.80 Ã— P0_phi`.
- `#gMe/#tPe.cs` â€” `#2Oe()` / `#pPe()`: At the top ring (pMax), the BiCurve boundary has non-zero moment extent â†’ the horizontal cap line is drawn as a natural part of the envelope.
- The reference does NOT draw the cap as a separate explicit segment; it emerges from the ring intersection at the capped Pmax level, matching our approach.

## PM Curve Merged Envelope Loop

### Architecture (Updated 2026-05-09)

The PM curve is now built as a **single closed polyline loop** per curve type, not as separate positive/negative branches:

1. Build 70 axial-level rings from Pmin to Pmax
2. At each ring, find max moment (positive) and min moment (negative) intersections
3. Merge into loop: **positive ascending** (Pminâ†’Pmax) â†’ **negative descending** (Pmaxâ†’Pmin)
4. The renderer closes the loop (`LineTo` back to first point)

This eliminates the need for separate top/bottom cap segments because:
- **Top cap**: Last positive point at Pmax connects directly to first negative point at Pmax (natural loop transition)
- **Bottom connection**: Last negative point at Pmin connects back to first positive point at Pmin (loop close)

GroupKey values:
- `"DesignCapacity"` â€” single closed loop for the design (Ï†-factored, ACI-capped) curve
- `"NominalCapacity"` â€” single closed loop for the nominal (unfactored, uncapped) curve
## Task 13: ACI Trim Segment Recheck Against CoreAssets

### Files Inspected

Current WPF/data flow:
- `ControlPointBuilderService`: builds capped display control points and nominal/design display values.
- `PmCurveBuilderService`: builds PMx/PMy and angle-based PM envelopes.
- `DiagramDataService`: routes `DiagramControlPointSet` to PM DTOs.
- `DiagramCanvas2D`: draws grouped control points as straight polyline segments; no spline/Bezier smoothing is applied.
- `PMDiagramViewModel` and `ResultViewModel`: bind the angle-based PM DTO to the chart.

Decompiled CoreAssets reference:
- `StructurePoint/CoreAssets/AppManager/Column/Reporting/Diagrams/Drawing/AxialLoadMomentDiagramGenerator.cs`
  - `#eEe(...)`: prepares nominal/factored `UniCurve` data and passes `RedFactPhiA` to the drawing engine.
- `StructurePoint/CoreAssets/AppManager/Column/Reporting/Diagrams/Drawing/AxialLoadMomentDrawingEngine.cs`
  - `#oEe(...)`: draws factored and nominal PM curves separately.
  - `#NEe(...)`: splits factored/design points below and above the ACI compression limit, and inserts an interpolated cap point when crossing the cap level.
  - `#0Ee(...)` / `#ZEe(...)`: interpolates the moment coordinate exactly at the cap axial load.
  - `#5Ee(...)`: draws the factored below-cap curve and top/cap collection as separate drawing collections.
  - `#sEe(...)` / `#9Ee(...)`: draws Pmax/Pmin reference lines separately from the capacity curve.
- `#gMe/#tPe.cs`
  - `#3Oe(...)`: constructs 70 axial-load `UniCurve` rows for a selected PM angle.
  - `#pPe(...)`: intersects each 36-point `BiCurve` ring with the selected PM angle and extracts positive/negative moment sides.
  - `#oPe(...)` / `#9Oe(...)`: refines top/bottom transition rows by bisection.

No previous HTML smoke-test implementation was found in the repository search. The comparison was therefore made against the CoreAssets decompiled implementation and the current WPF services.

### Root Cause Found

The WPF PM builder was using `ControlPoint.DisplayP`, `DisplayMx`, and `DisplayMy` for the design curve. Those values are already ACI-capped for the displayed design surface. At the compression cap, that creates a flat plateau of multiple points with `DisplayP = Pmax_design`, including pure-compression-like points with very small moment. The angle PM builder then sampled this capped display plateau and treated the plateau endpoint as the ACI trim intersection.

Observed before the final correction on the sample 700 x 700 mm column:
- At `theta = 0 deg`, the two cap points were near `Mtheta = +/-0.515 kN-m` at `P = 8894.472 kN`.
- The next lower sampled points were near `Mtheta = +/-622.491 kN-m` at `P = 8690.270 kN`.
- This proved the cap endpoint was coming from the capped plateau/pure-compression region, not from the raw factored design curve intersection at the ACI cap.

The chart renderer was not the cause: `DiagramCanvas2D` draws straight segments only and does not smooth the PM capacity curve.

### Corrected Implementation

`PmCurveBuilderService` now reconstructs raw factored design diagram coordinates from the preserved nominal display values and phi:

```text
P_design_raw  = Phi * P_nominal_display
Mx_design_raw = Phi * Mx_nominal_display
My_design_raw = Phi * My_nominal_display
```

The design PM envelope is built from these raw factored coordinates, then trimmed at:

```text
Pmax_design_ACI = DesignCompressionLimitDisplay
```

For the angle-based PM chart:
1. Build positive and negative branches from raw factored coordinates.
2. Exclude the top sampled level from the generic axial-level scan.
3. Intersect the selected PM slice with the raw factored ring at `Pmax_design_ACI`.
4. Select max/min `Mtheta` as the right/left trim endpoints.
5. Emit those two endpoints as adjacent control points at the same exact P value.
6. Do not pass the trim segment through any smoothing pipeline. No PM smoothing pipeline exists.

Observed after correction:
- `theta = 0 deg`: trim cap endpoints are `Mtheta = +/-574.644 kN-m` at `P = 8894.472 kN`.
- `theta = 30 deg`: trim cap endpoints are `Mtheta = +/-550.146 kN-m` at `P = 8894.472 kN`.
- `theta = 90 deg`: trim cap endpoints are `Mtheta = +/-574.644 kN-m` at `P = 8894.472 kN`.

Only two design capacity points sit at the cap level, and they are consecutive in the design curve. The dashed Pmax reference line remains a separate reference item.

